# Holberton School: Web back end

Backend / API coursework. Some tasks use MongoDB under the `NoSQL/` directory.

## MongoDB (NoSQL)

Scripts are run with the legacy shell, for example:

    cd NoSQL
    cat 0-list_databases | mongo

Ensure MongoDB is running and use the environment (e.g. Holberton sandbox) where `mongo` is available.
